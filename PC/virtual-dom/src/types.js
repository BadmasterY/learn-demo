/**
 * used to mark the diff type
 */
const REPLACE = 0;
const REORDER = 1;
const PROPS = 2;
const TEXT = 3;

const REMOVE = 0;
const INSERT = 1;

export default {
    REPLACE,
    REORDER,
    PROPS,
    TEXT,
    REMOVE,
    INSERT,
};